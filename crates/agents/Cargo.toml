[package]
name = "latoile-agents"
description = "Agent channel: ACP sessions, supervised spawn, permission queue. Implements core ports."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
