[package]
name = "latoile-vault"
description = "Encrypted secrets: envelope encryption, root key outside the database. Implements core ports."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
