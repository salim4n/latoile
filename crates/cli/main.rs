//! The `latoile` binary. Composition root: loads configuration, runs
//! migrations, wires adapters to ports, and starts the server.

fn main() {
    eprintln!("latoile: not implemented yet — see docs/architecture-spec.md");
    std::process::exit(1);
}
