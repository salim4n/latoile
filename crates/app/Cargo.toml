[package]
name = "latoile-app"
description = "Use cases: orchestration of the domain through core ports. No HTTP, no SQL at the edges."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
